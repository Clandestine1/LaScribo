.posts-wrapper.transitions-enabled.masonry-container.boxy.infinite-scroll
.col-xs-12.row
  -@posts.each do |post|
    .post.animate.hvr-grow.col-xs-4.boxy 
      .post-head
      .image.center-block
        =link_to (image_tag post.image.url(:medium), class:'img-responsive'), post_path(post)
      %p.caption
        =post.caption
        .name
          Boss Shauna
      
       